{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default PassKeeper permissions",
  "windows": ["main"],
  "permissions": [
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
